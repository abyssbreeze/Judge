#include<bits/stdc++.h>
using namespace std ;


int main(){
      cin.sync_with_stdio(0) ;
      cin.tie(0) ;

      string str ;
      int len ;
      getline(cin,str) ;
      len = str.size() ;
      cout<<str<<"\n" ;
      for (int i = 0; i < len; i++)
      {
        cout<<"~" ;
      }

  return 0 ;
}
